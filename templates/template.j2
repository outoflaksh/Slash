from flask import Flask, jsonify
from utils import generate_data

app = Flask(__name__)

{% for spec in specs %}

@app.route("{{ spec["url"] }}")
def {{ spec["url"][1:] + "_url"}}():
    return jsonify(generate_data({{ spec["size"] }}, {{ spec["schema"] }}))
{% endfor %}

if __name__ == "__main__":
    app.run(debug=True)
